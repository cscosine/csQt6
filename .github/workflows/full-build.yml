## Auto-generated code, please to not edit!

name: Full Build

on:
  # 1️⃣ Run on push / PR to main branch or tag v*.*.*, the latter will trigger release
  push:
    branches: [ "cs-main" ]
    tags:
      - 'v*.*.*'
  pull_request:
    branches: [ "cs-main" ]

  # 2️⃣ Manual trigger (UI + API)
  workflow_dispatch:
    inputs:
      source_repo:
        description: "Upstream repository"
        required: false
      source_branch:
        description: "Upstream branch"
        required: false
      source_commit:
        description: "Upstream commit"
        required: false

  # 3️⃣ Scheduled (weekly)
  schedule:
    - cron: "0 3 * * 1"   # Every Monday at 03:00 UTC

permissions:
  contents: write

# 🔒 Prevent duplicate / overlapping runs
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false # keep it false, early exit mechanism + debounce will prevent multiple builds

jobs:
  windows-build-info:
    name: Check Clang and System Info Windows
    runs-on: windows-latest
    steps:
      - name: Setup MSVC environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Show OS and compiler info
        shell: pwsh
        run: |
          Write-Host "=== OS Information ==="
          systeminfo | Select-String "OS Name","OS Version"

          Write-Host ""
          Write-Host "=== Visual Studio Installation ==="
          if (Get-Command vswhere -ErrorAction SilentlyContinue) {
            vswhere -latest -products * `
              -property installationPath
          } else {
            Write-Host "vswhere not found"
          }

          Write-Host ""
          Write-Host "=== MSVC Compiler (cl.exe) ==="
          if (Get-Command cl.exe -ErrorAction SilentlyContinue) {
            cl
          } else {
            Write-Host "cl.exe not found"
          }

          Write-Host ""
          Write-Host "=== Clang (clang-cl) ==="
          if (Get-Command clang-cl -ErrorAction SilentlyContinue) {
            clang-cl --version
          } else {
            Write-Host "clang-cl not found"
          }

  linux-build-info:
    name: Check Clang and System Info Linux
    runs-on: ubuntu-latest

    steps:
      - name: Show OS and compiler info
        run: |
          echo "=== OS Information ==="
          cat /etc/os-release || true
          echo ""
          echo "=== Clang Version ==="
          clang++ --version || echo "clang++ not found"
          echo ""
          echo "=== GCC Version (if installed) ==="
          gcc --version || echo "gcc not found"
          echo ""
          echo "=== CMake Version (if installed) ==="
          cmake --version || echo "cmake not found"
          echo ""
          echo "=== Ninja Version (if installed) ==="
          ninja --version || echo "ninja not found"

  trigger-info:
    name: Trigger info
    runs-on: ubuntu-latest

    steps:
      - name: Detect trigger
        id: trigger
        run: |
          echo "event_name=${{ github.event_name }}" >> $GITHUB_OUTPUT

          if [[ "${{ github.event_name }}" == "schedule" ]]; then
            echo "trigger=scheduled" >> $GITHUB_OUTPUT
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "trigger=manual-or-api" >> $GITHUB_OUTPUT
          elif [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "trigger=pull-request" >> $GITHUB_OUTPUT
          else
            echo "trigger=push" >> $GITHUB_OUTPUT
          fi

      - name: Print trigger info
        run: |
          echo "Event name : ${{ github.event_name }}"
          echo "Trigger    : ${{ steps.trigger.outputs.trigger }}"
          echo "Ref        : ${{ github.ref }}"
          echo "SHA        : ${{ github.sha }}"
          echo "Source repo:    ${{ github.event.inputs.source_repo }}"
          echo "Source branch:  ${{ github.event.inputs.source_branch }}"
          echo "Source commit:  ${{ github.event.inputs.source_commit }}"

  guard:
    name: Early-Exit Guard
    runs-on: ubuntu-latest

    outputs:
      should_build: ${{ steps.decision.outputs.should_build }}

    steps:
      - name: Debounce # prevent multiple quick successive runs with dependent and main repo updates
        run: sleep 60

      # checkout myself
      - name: checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # checkout other deps

      - name: checkout csProjectManager
        uses: actions/checkout@v4
        with:
          repository: cscosine/csProjectManager.git
          path: csProjectManager
          ref: cs-main
          fetch-depth: 1
          token: ${{ secrets.ACTIONS_ORG_ACCESS }}

      - name: checkout qt6
        uses: actions/checkout@v4
        with:
          repository: qt/qt5.git
          path: qt6
          ref: v6.10.2
          fetch-depth: 1

      # ---------------------------------------------------
      # Compute current SHAs
      - name: Compute current SHAs
        id: shas
        run: |
          echo "csQt6=$(git rev-parse HEAD)" >> shas.txt
          # all other deps...
          echo "cscosine/csProjectManager.git=$(git -C csProjectManager rev-parse HEAD)" >> shas.txt
          echo "qt/qt5.git=$(git -C qt6 rev-parse HEAD)" >> shas.txt

          cat shas.txt

      # ---------------------------------------------------
      # Restore previous SHAs from cache (cheap persistent store)
      - name: Restore previous SHAs
        id: cache
        uses: actions/cache@v4
        with:
          path: shas-prev.txt
          key: deps-sha-${{ github.ref_name }}-${{ github.run_id }}
          restore-keys: |
            deps-sha-${{ github.ref_name }}-

      # ---------------------------------------------------
      # Decide whether to build
      - name: Decide whether to build
        id: decision
        run: |
          # Always build on tag
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            echo "should_build=true" >> $GITHUB_OUTPUT
            echo "💡 Trigger is a tag, will build"
            exit 0
          fi

          if [ ! -f shas-prev.txt ]; then
            echo "should_build=true" >> $GITHUB_OUTPUT
            echo "💡 No previous SHAs found → will build"
            exit 0
          fi

          if cmp -s shas.txt shas-prev.txt; then
            echo "should_build=false" >> $GITHUB_OUTPUT
            echo "💤 SHAs unchanged → skipping build"
          else
            echo "should_build=true" >> $GITHUB_OUTPUT
            echo "💡 SHAs changed → will build"
          fi

      # ---------------------------------------------------
      # Save current SHAs for next run
      - name: Prepare SHAs for cache
        run: cp shas.txt shas-prev.txt

      - name: Save current SHAs to cache
        if: steps.decision.outputs.should_build == 'true'
        uses: actions/cache@v4
        with:
          path: shas-prev.txt
          key: deps-sha-${{ github.ref_name }}-${{ github.run_id }}

      # ---------------------------------------------------
      # 🔹 Show nice summary in Actions UI
      - name: Early-Exit Summary
        run: |
          echo "### 🔹 Early-Exit Guard Summary"
          echo ""
          echo "Trigger type : ${GITHUB_EVENT_NAME}"
          echo "Ref          : ${GITHUB_REF}"
          echo "Current SHAs :"
          cat shas.txt
          echo ""
          if [ "${{ steps.decision.outputs.should_build }}" == "true" ]; then
            echo "✅ Build will proceed"
          else
            echo "💤 Build skipped (early-exit)"
          fi

  build-linux-linux-gcc:
    needs: guard
    if: needs.guard.outputs.should_build == 'true'

    runs-on: ubuntu-latest
    steps:

    ##### UNCOMMENT AND MOVE THESE LINES IF YOU NEED A TERMINAL TO INTERACT AND INSPECT THE JOB
    # - name: Start tmate session
    #   uses: mxschmitt/action-tmate@v3


    # checkout myself
    - name: checkout
      uses: actions/checkout@v4

    # ---------------------------------------------------
    # checkout and commands
    - name: checkout csProjectManager
      uses: actions/checkout@v4
      with:
        repository: cscosine/csProjectManager.git
        path: csProjectManager
        ref: cs-main
        token: ${{ secrets.ACTIONS_ORG_ACCESS }}

    - name: Install prerequisites for Linux
      run: ./installRequirements-linux.sh on linux

    - name: checkout qt6
      uses: actions/checkout@v4
      with:
        repository: qt/qt5.git
        path: qt6
        ref: v6.10.2

    - name: Init Qt Submodules (linux)
      run: ./init-repository-linux.sh on linux

    # ---------------------------------------------------

    # ---------------------------------------------------
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install Python dependencies
      run: pip install -r csProjectManager/requirements.txt

    - name: Run all pre-commit hooks
      run: python3 project.py --install-hooks --run-hooks
      working-directory: .
    # ---------------------------------------------------


    # ---------------------------------------------------
    # build section
    - name: Build linux-linux-gcc-qt6 (1 / 1)
      run: ../build-repository-linux.sh &> ../qt6-linux-linux-gcc-1-build_output.log
      working-directory: qt6

    - name: Upload build log
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: qt6-linux-linux-gcc-1-build_output.log
        path: qt6-linux-linux-gcc-1-build_output.log

    - name: Show last lines on failure
      if: failure()
      run: tail -n 200 qt6-linux-linux-gcc-1-build_output.log
      working-directory: .

    # ---------------------------------------------------

    # ---------------------------------------------------
    # zip installation section
    - name: Zip Installations linux-linux-gcc
      run: |
        if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
          VERSION=${GITHUB_REF_NAME}
        else
          VERSION=$(date +'%Y%m%d-%H%M%S')
        fi

        echo "VERSION=$VERSION" >> $GITHUB_ENV
        echo "Using VERSION=$VERSION"

        (
          cd install/linux-gcc/
          zip -r "$GITHUB_WORKSPACE/bundle-csQt6-${VERSION}-qt6-linux-linux-gcc.zip" qt6
        )
      shell: bash
    # ---------------------------------------------------

    # ---------------------------------------------------
    # prepare artifacts section
    - name: Upload Installation Artifacts linux-linux-gcc
      uses: actions/upload-artifact@v4
      with:
        name: linux-linux-gcc
        if-no-files-found: error
        path: |
          bundle-csQt6-${{ env.VERSION }}-qt6-linux-linux-gcc.zip
    # ---------------------------------------------------

  build-windows-windows-msvc2022-x64:
    needs: guard
    if: needs.guard.outputs.should_build == 'true'

    runs-on: windows-latest
    steps:


    # checkout myself
    - name: checkout
      uses: actions/checkout@v4

    # ---------------------------------------------------
    # checkout and commands
    - name: checkout csProjectManager
      uses: actions/checkout@v4
      with:
        repository: cscosine/csProjectManager.git
        path: csProjectManager
        ref: cs-main
        token: ${{ secrets.ACTIONS_ORG_ACCESS }}

    - name: checkout qt6
      uses: actions/checkout@v4
      with:
        repository: qt/qt5.git
        path: qt6
        ref: v6.10.2

    - name: Init Qt Submodules (windows)
      run: cmd /c init-repository-windows.bat on windows

    # ---------------------------------------------------

    # ---------------------------------------------------
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install Python dependencies
      run: pip install -r csProjectManager/requirements.txt

    - name: Run all pre-commit hooks
      run: python3 project.py --install-hooks --run-hooks
      working-directory: .
    # ---------------------------------------------------

    # ---------------------------------------------------
    # additional setup for windows
    - name: Setup MSVC environment
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: x64

    # note: keep setup cmake after msvc setup to have the correct cmake in path
    - name: Setup CMake
      uses: lukka/get-cmake@latest

    # ---------------------------------------------------

    # ---------------------------------------------------
    # build section
    - name: Build windows-windows-msvc2022-x64-qt6 (1 / 1)
      run: cmd /c ..\build-repository-windows.bat *> ../qt6-windows-windows-msvc2022-x64-1-build_output.log
      working-directory: qt6

    - name: Upload build log
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: qt6-windows-windows-msvc2022-x64-1-build_output.log
        path: qt6-windows-windows-msvc2022-x64-1-build_output.log

    - name: Show last lines on failure
      if: failure()
      run: Get-Content qt6-windows-windows-msvc2022-x64-1-build_output.log -Tail 200
      working-directory: .
    # ---------------------------------------------------

    # ---------------------------------------------------
    # zip installation section
    - name: Zip Installations windows-windows-msvc2022-x64
      shell: pwsh
      run: |
        if ($env:GITHUB_REF_TYPE -eq "tag") {
          $VERSION = $env:GITHUB_REF_NAME
        } else {
          $VERSION = Get-Date -Format "yyyyMMdd-HHmmss"
        }

        "VERSION=$VERSION" | Out-File -FilePath $env:GITHUB_ENV -Append
        Write-Host "Using VERSION=$VERSION"

        # zip qt6

        $src = "$env:GITHUB_WORKSPACE\install\windows-msvc2022-x64\qt6"
        $dst = "$env:GITHUB_WORKSPACE\bundle-csQt6-$VERSION-qt6-windows-windows-msvc2022-x64.zip"
        Write-Host "*** src $src"
        Write-Host "*** dst $dst"

        if (Test-Path $dst) {
            Remove-Item $dst
        }


        if (-not (Test-Path $src)) {
          throw "Source path does not exist: $src"
        }

        Compress-Archive -Path $src -DestinationPath $dst

    # ---------------------------------------------------


    # ---------------------------------------------------
    # prepare artifacts section
    - name: Upload Installation Artifacts windows-windows-msvc2022-x64
      uses: actions/upload-artifact@v4
      with:
        name: windows-windows-msvc2022-x64
        if-no-files-found: error
        path: |
          bundle-csQt6-${{ env.VERSION }}-qt6-windows-windows-msvc2022-x64.zip
    # ---------------------------------------------------


  # ---------------------------------------------------
  # create release section
  create-release:
    runs-on: ubuntu-latest
    needs: [ build-linux-linux-gcc, build-windows-windows-msvc2022-x64 ]  # waits for all builds
    if: ${{ github.ref_type == 'tag' }}   # only on tag
    steps:

      - name: Download artifacts linux-linux-gcc
        uses: actions/download-artifact@v4
        with:
          name: linux-linux-gcc
          path: linux-linux-gcc

      - name: Download artifacts windows-windows-msvc2022-x64
        uses: actions/download-artifact@v4
        with:
          name: windows-windows-msvc2022-x64
          path: windows-windows-msvc2022-x64

      - name: Create GitHub release and upload assets
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          fail_on_unmatched_files: true
          files: |
            linux-linux-gcc/*.zip
            windows-windows-msvc2022-x64/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  # ---------------------------------------------------
